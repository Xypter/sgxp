---
// Registration lives on the Register tab of /login (LoginForm.svelte, backed by
// Payload). This URL used to be an old Supabase-auth sign-up page; it now just
// forwards any old links/bookmarks to the real form.
return Astro.redirect('/login?tab=register', 301);
---
