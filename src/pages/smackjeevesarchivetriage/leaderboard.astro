---
import FrontPageLayout from "../../layouts/FrontPageLayout.astro";
import LeaderboardBoard from "../../components/archive/LeaderboardBoard.svelte";

// Publicly viewable, like the triage table itself.
---

<FrontPageLayout title="Archive Leaderboard">
  <div class="leaderboard-page">
    <div class="leaderboard-header">
      <h1>Archive Triage Leaderboard</h1>
      <p>Who's identified the most entries, and who's reviewed the most.</p>
    </div>

    <LeaderboardBoard client:only="svelte" />
  </div>
</FrontPageLayout>

<style>
  .leaderboard-page {
    width: 100%;
    padding: 20px 1rem;
  }

  .leaderboard-header {
    background: var(--page-color);
    border: var(--border-width, 2px) var(--border-style, solid) color-mix(in srgb, var(--page-color) 80%, white);
    box-shadow: var(--box-shadow);
    padding: 16px 20px;
    margin-bottom: var(--gap, 20px);
  }

  .leaderboard-header h1 {
    font-family: 'saira';
    font-weight: 800;
    font-size: 24px;
    color: var(--font-color);
    margin: 0 0 8px 0;
  }

  .leaderboard-header p {
    font-family: 'saira';
    font-size: 14px;
    color: var(--font-color);
    opacity: 0.8;
    margin: 0;
  }
</style>
